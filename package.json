{
  "name": "yoump3",
  "version": "1.0.0",
  "description": "API que convierte videos de YouTube a MP3 y devuelve una URL pública de descarga",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test \"test/**/*.test.js\"",
    "vercel-build": "node scripts/fetch-binaries.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@vercel/blob": "^0.27.3",
    "express": "^4.21.2",
    "tar": "^7.5.19"
  }
}
